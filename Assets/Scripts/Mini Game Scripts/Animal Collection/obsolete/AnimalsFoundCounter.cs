using System.Collections;
using System.Runtime.CompilerServices;
using TMPro;
using UnityEngine;

public class AnimalsFoundCounter : MonoBehaviour
{
    //[SerializeField]
    //AnimalGameManager animalGameManager;

    //private int targetScore = 12;

    //private void Start()
    //{
    //    StartCoroutine(CountAnimals());
    //}

    //private void Update()
    //{
    //    if (animalGameManager.AnimalsFound == 12)
    //    {
    //        StopCoroutine(CountAnimals());
    //    }
    //}

    //private IEnumerator CountAnimals()
    //{
    //    while (animalGameManager.AnimalsFound < targetScore)
    //    {
    //        if (RaycastScript.easternStarlingClicked && !RaycastScript.wasEasternStarlingPreviouslyClicked)
    //        {
    //            animalGameManager.AnimalsFound += 1;
    //            animalGameManager.UpdateAnimalCounter();
    //            yield return new WaitForSeconds(1);
    //        }
            
    //        if (RaycastScript.whiteTailedDeerClicked && !RaycastScript.wasWhiteTailedDeerPreviouslyClicked)
    //        {
    //            animalGameManager.AnimalsFound += 1;
    //            animalGameManager.UpdateAnimalCounter();
    //            yield return new WaitForSeconds(1);
    //        }

    //        if (RaycastScript.bandedPennantDragonflyClicked && !RaycastScript.wasBandedPennantDragonflyPreviouslyClicked)
    //        {
    //            animalGameManager.AnimalsFound += 1;
    //            animalGameManager.UpdateAnimalCounter();
    //            yield return new WaitForSeconds(1);
    //        }

    //        if (RaycastScript.garterSnakeClicked && !RaycastScript.wasGarterSnakePreviouslyClicked)
    //        {
    //            animalGameManager.AnimalsFound += 1;
    //            animalGameManager.UpdateAnimalCounter();
    //            yield return new WaitForSeconds(1);
    //        }

    //        if (RaycastScript.baldEagleClicked && !RaycastScript.wasBaldEaglePreviouslyClicked)
    //        {
    //            animalGameManager.AnimalsFound += 1;
    //            animalGameManager.UpdateAnimalCounter();
    //            yield return new WaitForSeconds(1);
    //        }

    //        if (RaycastScript.paintedLadyButterflyClicked && !RaycastScript.wasPaintedLadyButterflyPreviouslyClicked)
    //        {
    //            animalGameManager.AnimalsFound += 1;
    //            animalGameManager.UpdateAnimalCounter();
    //            yield return new WaitForSeconds(1);
    //        }

    //        if (RaycastScript.asianCarpClicked && !RaycastScript.wasAsianCarpPreviouslyClicked)
    //        {
    //            animalGameManager.AnimalsFound += 1;
    //            animalGameManager.UpdateAnimalCounter();
    //            yield return new WaitForSeconds(1);
    //        }

    //        if (RaycastScript.beaverClicked && !RaycastScript.wasBeaverPreviouslyClicked)
    //        {
    //            animalGameManager.AnimalsFound += 1;
    //            animalGameManager.UpdateAnimalCounter();
    //            yield return new WaitForSeconds(1);
    //        }

    //        if (RaycastScript.raccoonClicked && !RaycastScript.wasRaccoonPreviouslyClicked)
    //        {
    //            animalGameManager.AnimalsFound += 1;
    //            animalGameManager.UpdateAnimalCounter();
    //            yield return new WaitForSeconds(1);
    //        }

    //        if (RaycastScript.muskratClicked && !RaycastScript.wasMuskratPreviouslyClicked)
    //        {
    //            animalGameManager.AnimalsFound += 1;
    //            animalGameManager.UpdateAnimalCounter();
    //            yield return new WaitForSeconds(1);
    //        }

    //        if (RaycastScript.snappingTurtleClicked && !RaycastScript.wasSnappingTurtlePreviouslyClicked)
    //        {
    //            animalGameManager.AnimalsFound += 1;
    //            animalGameManager.UpdateAnimalCounter();
    //            yield return new WaitForSeconds(1);
    //        }

    //        if (RaycastScript.northernMapTurtleClicked && !RaycastScript.wasNorthernMapTurtlePreviouslyClicked)
    //        {
    //            animalGameManager.AnimalsFound += 1;
    //            animalGameManager.UpdateAnimalCounter();
    //            yield return new WaitForSeconds(1);
    //        }
    //    }
    //}
}
